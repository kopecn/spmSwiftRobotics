Format Swift source files using swift-format:

1. Check if swift-format is available
2. Format all Swift files in Sources/ and Tests/ directories
3. Report which files were modified
4. Show any formatting issues or errors

Use the project's .swift-format configuration if it exists.
